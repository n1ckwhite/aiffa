export type ProgressHeaderProps = {
  overline: string;
  title: string;
  description: string;
  titleId: string;
  descriptionId: string;
  titleColor: string;
  textColor: string;
  accentLabel: string;
};
