import type { MiniCase } from "../../../types/miniCase";

export type MiniCaseCardProps = {
  item: MiniCase;
  isDark: boolean;
  borderColor: string;
  borderHoverColor: string;
  cardBg: string;
  baseShadow: string;
  hoverShadow: string;
  labelColor: string;
  titleColor: string;
  textColor: string;
  actionColor: string;
  prefersReducedMotion: boolean;
};
