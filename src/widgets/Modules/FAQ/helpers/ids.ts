export const getFaqSectionTitleId = (variantKey: string) => `faq-${variantKey}-title`;

export const getFaqQuestionId = (variantKey: string, idx: number) => `faq-${variantKey}-q-${idx}`;

export const getFaqAnswerId = (variantKey: string, idx: number) => `faq-${variantKey}-a-${idx}`;

